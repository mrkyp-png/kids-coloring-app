# 작업 규칙

- **git commit과 push는 사용자가 명시적으로 요청할 때만 한다.** 검증/테스트가 다 끝났어도 먼저 올리지 말고 요청을 기다린다.
- **파일을 수정하기 전에 무엇을, 왜 바꾸는지 먼저 설명하고 사용자 확인을 받는다.** 확인 없이 바로 편집하지 않는다.
- **배포(실제 서비스 중인 사이트)에 영향 가는 변경은 반드시 사전에 알린다.**

# 로컬 미리보기

- **`index.html`을 더블클릭해서 직접 열지 않는다.** 이모지 도안을 `assets/emoji/*.svg` 실제 이미지 파일로 불러오는 구조라서, `file://`로 열면 브라우저가 "서로 다른 출처"로 취급해 캔버스에 그림을 못 그린다(Goal/색칠 도안이 흰 화면으로 뜸). 버그 아님, 구조적 한계.
- **로컬 미리보기는 `미리보기.bat`(프로젝트 폴더)를 더블클릭한다.** `scripts/serve.js`로 로컬 서버(`http://localhost:8843`)를 띄운 뒤 브라우저를 자동으로 연다. 이 창을 닫으면 서버도 꺼진다.
- 실제 서비스 확인은 `https://mrkyp-png.github.io/kids-coloring-app/`.
