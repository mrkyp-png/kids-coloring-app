# 색칠공부 업그레이드 체크리스트

> 대상: `kids-coloring-app` (탭-채우기 PWA)
> 이번 라운드 목표: **난이도 10단계 × 2개 = 20개 도안 + 컬러바이넘버 방식 성공률 채점 시스템**

## 확정된 설계
- **정답색 매칭 방식**: 도안마다 영역별로 지정된 정답 색이 있음(컬러바이넘버). 사용자가 그 영역을 정답색으로 칠하면 성공.
- **종합점수**: 성공률을 그대로 점수화 (성공률 100% = 100점). 시간·실행취소 횟수는 반영 안 함.
- **점수별 피드백(5단계, 확정)**: 성공률 기준
  - 90~100% 🌟 완벽해요! 색깔박사예요
  - 75~89% 😄 아주 잘했어요
  - 60~74% 🙂 잘했어요
  - 40~59% 💪 조금만 더 힘내요
  - 0~39% 🌈 다시 도전해봐요
- **팔레트 확장**: 난이도가 올라갈수록 선택 가능한 색상 수 증가(4색→11색 점증). 각 단계 정답색은 반드시 그 단계 팔레트 안에 포함되도록 설계(안 그러면 영구히 못 맞추는 영역 발생).
- **난이도 기준**: 영역(채색 가능 영역) 개수 기준 오름차순. 적을수록 쉬움.

## Phase 1 — 기존 도안 분석
- [ ] 헤드리스 브라우저로 기존 16개 도안의 실제 영역 개수 측정 (지금 로직인 connected-component labeling 그대로 사용)
- [ ] 영역 개수 기준으로 16개를 난이도 순으로 정렬

## Phase 2 — 20개로 확장
- [ ] 신규 도안 4개 설계 (난이도 구간 빈 곳 채우기, 영역 수 목표치에 맞춰 설계)
- [ ] 최종 10단계 × 2개 매핑 확정

## Phase 3 — 컬러바이넘버 데이터 작성
- [ ] 각 도안의 영역마다 앵커 포인트(영역 내부 좌표) + 정답색 지정
  - 앵커 포인트는 라벨링 알고리즘으로 자동 추출한 영역 중심점 사용 (수작업 좌표 추정 최소화)
- [ ] 팔레트 단계별 색상 구성표 확정 (4→11색) 및 각 도안 정답색이 자기 단계 팔레트에 포함되는지 검증

## Phase 4 — 로직 구현 (app.js)
- [ ] 영역별 정답색 매칭 로직 추가 (탭한 영역 라벨 → 정답색 조회 → 선택색과 비교)
- [ ] 성공률 계산: 정답색으로 칠해진 영역 수 / 전체 영역 수
- [ ] 종합점수 표시 UI 추가 (완성 결과 화면)
- [ ] 기존 5단계 피드백을 새 점수 기준으로 연결
- [ ] 난이도별 팔레트 크기 적용 (`renderPalette`가 현재 도안의 난이도를 참조)
- [ ] 갤러리에 난이도 표시(별점/숫자 등) 추가

## Phase 5 — 검증
- [ ] 헤드리스 브라우저로 20개 도안 전수 테스트: 정답색 전부 칠했을 때 성공률 100% 나오는지 확인
- [ ] 슬리버(겹침 미세 영역) 재발 여부 확인 (신규 4개 포함)
- [ ] 실기기 확인 (선택)

---
## 완료 결과 (2026-08-08)
- 20개 도안, 10단계 x 2개, 슬리버 없이 깨끗하게 완성 (heart/cloud=Lv1 ... flower/strawberry=Lv10)
- 신규 4종: Cloud, Ladybug, Grapes, Strawberry (기존 6종은 sprinkle/confetti 방식으로 영역 수 조정: fish/balloon/apple/butterfly/boat/icecream)
- 컬러바이넘버 채점: 기존 goal-canvas 자동 배정색을 그대로 정답 데이터로 재사용 (수작업 좌표 불필요)
- 팔레트 4→11색 단계적 확장(PALETTE_SIZE_BY_LEVEL), 모든 도안 정답색이 자기 단계 팔레트에 포함되는지 자동 검증 완료
- 5단계 평가 문구 확정 및 적용
- 전 세계 출시 목표에 맞춰 전체 UI 텍스트 영어(유아 수준)로 전환 (index.html, app.js, templates.js, manifest.json)
- 검증 도구 상시 보관: `scripts/analyze-templates.js`(영역 수 측정), `scripts/validate-all.js`(20개 전수 100% 검증) — 이후 도안 추가/수정 시 재사용 가능
- sw.js 캐시 버전 v1→v2 갱신

## 2차 업그레이드 (2026-08-08, 같은 날 추가 요청)
사용자가 원한 실제 구조: **10단계 × 10개(총 100개) + 레벨 지도 화면 + 순차 잠금 + 레벨 Clear 표시**로 재설계.

### Phase 7 — 레벨 지도/잠금/Clear
- 새 `#map-screen`(홈): 10개 레벨 노드, 잠금 아이콘, ✅CLEAR 배지, x/10 진행률 표시
- `#gallery-screen`은 "레벨별 도안 목록" 화면으로 전환(뒤로가기로 맵 복귀)
- localStorage `clearedTemplates`에 완료한 도안 id 저장(점수 무관, 저장 확인 시 1회 기록) → 레벨 10개 전부 있어야 Clear, 레벨N+1은 레벨N Clear해야 잠금 해제
- 헤드리스 테스트로 "레벨1 10개 완료 → Clear 배지 + 레벨2 잠금 해제" 흐름 검증 완료

### Phase 8 — 100개 도안 확충
- 기존 20개(레벨당 2개) → 80개 추가 제작(레벨당 10개), 전부 실제 사물과 닮은 형태로 작성(동물/과일/사물/탈것/자연/장면)
- 제작 중 발견: 기존 20개 중 일부(sun/tree/flower/duck)에도 처음부터 눈에 안 보이는 미세 슬리버가 숨어있었음 → 개별 좌표 수정 대신 **핵심 로직에 `MIN_REGION_SIZE` 필터 추가**(10px 미만 조각은 채점 대상에서 자동 제외)로 100개 전체를 한 번에 안전하게 처리
- 실측 영역 수 기준으로 100개를 10단계 x 10개로 재배정(추측이 아니라 `scripts/analyze-templates.js` 측정값 기준 오름차순 정렬)
- 팔레트 크기(4~11색)는 기존 로직 그대로 재사용 유효함을 재검증
- 최종 전수 검증: 슬리버 0건, 팔레트-정답색 커버리지 100%, 정답대로 칠하면 100점 100/100건 통과
- sw.js 캐시 버전 v2→v3 갱신

### 재사용 가능한 검증 스크립트 (`scripts/`)
- `analyze-templates.js` — 전체 도안 영역 수 측정
- `sliver-sweep.js` — 10px 미만 미세 슬리버 전수 탐지
- `check-palette-coverage.js` — 정답색이 팔레트에 다 포함되는지 확인
- `validate-all.js` — 정답대로 칠하면 100점 나오는지 전수 확인
- `check-sizes.js <id...>` — 특정 도안의 영역별 픽셀 크기 확인(디버깅용)
