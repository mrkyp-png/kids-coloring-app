# Coloring Fun — 개발 히스토리

> 이 문서는 프로젝트 시작부터 지금까지의 작업을 날짜순으로 정리한 것입니다.
> **매 작업 세션 후 계속 업데이트됩니다.** (요청: "히스토리 처음부터 지금까지 정리하고, 계속 업데이트하자")

- 저장소: `mrkyp-png/kids-coloring-app` (GitHub Pages)
- 배포 주소: https://mrkyp-png.github.io/kids-coloring-app/
- 로컬 미리보기: `미리보기.bat` 더블클릭 (`index.html` 직접 열면 안 됨 — 도안이 흰 화면으로 뜸)
- 체크포인트 태그: `색칠rev.0`(2026-08-10), `색칠rev.1`(2026-08-11), `색칠rev.2`(2026-08-11), `색칠rev.3`(2026-08-11)

---

## 2026-08-09 — 프로젝트 시작
- 첫 커밋: 100개 도안(10레벨×10개) + 레벨/점수 시스템을 가진 색칠 PWA
- 기기별로 렌더링이 달라지는 문제 발견 → 시스템 이모지 폰트 대신 실제 Twemoji SVG 파일을 내장하는 방식으로 전면 교체
  - 이 과정에서 벽(선) 감지 알고리즘의 방향 편향, 배경 판정 로직, SVG viewBox 여백 부족 등 진짜 버그 3개 발견/수정 → 도안의 92%가 색칠 가능해짐

## 2026-08-10 — 게임성 다지기
- 탭 정밀도 개선(가느다란 영역도 근처 탭이면 인식)
- 배경음악을 레벨 구간별 3곡 → 나중에 "아기상어 같은 느낌"을 원해서 CC0 라이선스 곡 1개로 단순화
- 4개 파이널 보스(당시 손그림 SVG) 퀄리티 개선 시도 → 그래도 "제일 걱정된다"는 평가로, 같은 세션 후반에 손그림을 완전히 버리고 **실제 Twemoji 캐릭터**(요정/인어/마법사/히어로)로 교체, 장식(소품+반짝이)으로 영역 수를 50개 수준까지 늘려 진짜 "보스"답게 어렵게 만듦
- 폰트: Fredoka(둥글고 통통한 아동용 폰트) 전체 적용
- 모드별 점수/클리어 기록이 전역으로 저장되던 버그 수정(모드 바꾸면 다른 모드 클리어가 안 잠기던 문제) — 모드별 완전 분리 저장으로 전환
- Done! 버튼 흐름 단순화: 중간 확인 모달 삭제, 누르면 즉시 성공/실패 결과 표시
- 색상 팔레트를 색맹 시뮬레이션까지 검증한 11색 세트로 교체
- 타이머 최종 확정: 쉬움20/보통15/어려움10/매우어려움5분
- 모드별로 정답색 배치를 랜덤화(재도전 시 다른 색 배치, 단 쉬움 모드는 실제 사물색 유지)
- 음성(TTS) 칭찬 멘트 추가(레벨클리어/Next/Back), 아이 목소리에 최대한 가깝게 피치/속도 튜닝
- **체크포인트 `색칠rev.0`** 태그

## 2026-08-11 (세션 1) — 실사용 버그 수정 + `색칠rev.1`
- 클리어한 보스가 다른 모드 클리어 후 다시 잠긴 것처럼 보이던 버그 수정(레벨타임 기록이 모드 구분 없이 저장되던 문제)
- 마법사/인어 보스의 검은 배경 영역 과다 문제 완화, 보스 제한시간을 모드별로 차등(5/4/3/2분)
- 보스 음악(6.8MB)이 하나라도 실패하면 서비스워커 설치 전체가 실패하던 문제 수정(필수 파일만 원자적 캐싱)
- 히어로걸 광선 이펙트 삭제, 색칠 커서를 연필 모양으로 변경
- **체크포인트 `색칠rev.1`** 태그

## 2026-08-11 (세션 2, 오늘 이어서) — 모바일 버그 대공세 + 디자인 리뉴얼 + 출시 준비
### 모바일 전용 버그 수정
- 음성 멘트가 폰에서 안 나오던 문제 → 3단계로 해결: ① `speechSynthesis` 동기 호출(지연 제거), ② 크롬 GC 버그 방지(참조 유지), ③ TTS 자체가 없는 기기는 축하 차임(합성음)으로 자동 대체
- 화면 꺼도 배경음악이 안 멈추던 문제 수정
- 시간초과 후 클리어 체크표시가 잘못 남는 실제 렌더링 순서 버그 발견/수정(재현 스크립트로 검증)
- Next/Back 버튼 음성 1초 지연 → 화면 진입 시 미리 예열해서 해결
- 그림 전환 시 이전 그림이 잠깐 남아 보이던 잔상 버그 수정

### 앱스토어 출시 준비
- 가격/카테고리 결정: **$1 일회성 결제(평생 이용)**, "유아 전용"이 아닌 **일반 패밀리 앱**으로 등록(Hard/Very Hard가 어른도 어려워서, 난이도는 유지하고 "부모와 같이 도전"으로 포지셔닝)
- 최초 실행 온보딩 추가: 보호자 확인 게이트(간단 덧셈 문제) → 이용안내 동의(광고 없음/개인정보 안내 + `privacy.html` 링크)
- `privacy.html` 개인정보처리방침 신규 작성(Twemoji CC-BY 4.0 출처표시 포함)
- 랭킹을 **로컬 → 전 세계 공용**으로 전환: 실명/자유입력 국가 대신 **닉네임+국기 이모지**만 공개(개인정보 보호), 백엔드는 Firebase Realtime Database(REST, SDK 없음) — **아직 실제 프로젝트 URL 미연동, 다음 단계로 대기 중**
- 닉네임/국기는 한 번 등록하면 수정 불가(잠금 UI)

### 디자인 전면 리뉴얼("촌스럽다" 피드백 대응)
- 레벨박스/갤러리카드: 크림 단색 → 원색 무지개 순환 색 + 대표 이모지 + 키캡 입체감
- 표지 화면: 떠다니는 반짝이 장식 추가, 배경 그라디언트 풍성하게
- **다크모드 완전 제거** — 폰이 다크모드여도 항상 밝은 베이지 테마로 통일
- 로딩 화면(요정 마스코트 + 스피너) 신규 추가
- 앱 아이콘: 색깔 점 4개 → 요정 캐릭터 + 반짝이 4개(스토어 출시용, maskable 안전영역 적용)
- 소리 버튼을 색칠화면 → 맵 화면(Ranking 알약 밑)으로 이동, 작게
- 맵 화면 헤더 정리: "Coloring Fun" 제목/부제 삭제, 그 자리엔 진행률 문구만, "Reset Mode" 버튼을 Ranking과 같은 크기/스타일로 그 밑에 배치
- 보스 카드(잠김 상태)/툴바 버튼(Undo/Clear/Done!)/모달 버튼에 통일된 키캡 입체감 + 색 포인트 적용
- 보스 클리어 화면의 "Print My Art/Print Blank Page" 버튼 삭제

### 색칠 정확도 개선
- 쉬움 모드 정답색이 **실제 이모지 원본 색과 정확히 일치**하도록 수정(기존엔 앱 팔레트로 스냅되어 미묘하게 달랐음)
- 상어 이빨처럼 흰색 계열 정답색이 배경과 안 구분되던 문제 수정(크림색으로 대체)
- 롤리팝처럼 같은 도안 안에 비슷한 색이 3개 이상이면 자동으로 병합(과하게 세분화된 색칠 방지) — 다른 도안(무지개/수박/사과/공작)으로 과병합 없는지 검증 완료
- **체크포인트 `색칠rev.2`** 태그

### 아직 안 된 것 / 다음 단계
- Firebase 프로젝트 생성 + DB URL 전달(랭킹 실제 작동을 위해 필요)
- Google Play Console 계정 가입/본인인증
- 스토어 리스팅 준비(아이콘/스크린샷/설명문/연령등급), 정산 계좌/세금 정보
- (사용자 예정) 앱 내 화면 디자인 추가 아이디어 구상 중

## 2026-08-11 (세션 3) — 다국어(i18n) 지원 추가
- 계기: "초기화면에서 페이지의 영어를 번역하시겠습니까" 팝업이 자꾸 뜬다는 사용자 문의 — 원인은 UI 문구(도안 이름 100개 포함)가 전부 영어로 하드코딩돼 있었기 때문(일부 alert만 예외적으로 한국어라 언어가 뒤섞여 있기도 했음)
- `i18n.js` 신규 추가: 한국어/영어/일본어/중국어(간체)/스페인어 5개 언어 사전 + `templates.js` 도안 100개·보스 4종 이름 번역 + 국가명(랭킹 국기 선택) 번역
- 표지 화면 우상단 🌐 버튼으로 언어 선택(드롭다운) → `localStorage`에 저장, 다음 방문부터 유지, 최초 방문 시 `navigator.language`로 자동 감지
- `index.html`(정적 문구에 `data-i18n` 속성) / `app.js`(칭찬 메시지·랭킹 안내·타이머·alert/confirm 등 동적 문구 전부 `I18N.t()` 호출로 교체) 수정
- Puppeteer(기존 Edge, headless)로 5개 언어 전체 화면(표지/온보딩 게이트+동의/맵/보스 섹션/갤러리/색칠 화면) 스크린샷 검수 — 콘솔 에러 없음, CJK/스페인어 텍스트 줄바꿈도 카드 밖으로 안 넘침 확인
- `<html lang>`도 언어 전환 시 함께 갱신 — 실제 콘텐츠 언어와 일치하므로 브라우저 번역 제안 팝업도 자연히 사라짐

### 색칠 정확도/UX 추가 개선(같은 세션, 사용자 플레이 피드백)
- "5레벨부터 비슷한 색끼리 섞여 있어 구분이 어렵다" — 보통/어려움/매우어려움 모드의 랜덤 색 배정(`seededRegionColors`)이 "이웃과 완전히 같은 색만 피함" → "이웃과 RGB 거리 100 이상 나는 색만 후보로, 후보가 없으면 그나마 가장 먼 색으로 fallback" 방식으로 변경(팔레트 최소 티어 4색에서도 색끼리 최소 거리가 108이라 후보 고갈 없음 확인)
- goal(정답) 이미지 확대 기능 추가: `goal-canvas-wrap`을 탭하면 같은 그림을 화면 꽉 차게 보여주는 모달이 열림(내부 해상도가 이미 640×640이라 확대해도 화질 손실 없음), 아무 곳이나 탭하면 닫힘 — 우하단에 🔍 배지로 탭 가능함을 표시
- "쉬움 보스(요정) 머리색이 얼굴색과 같이 나온다" — 원인은 같은 이미지 안 비슷한 색을 하나로 합치는 클러스터링(롤리팝 등 음영 표현 대응용)이 금발머리↔피부색처럼 실제로 비슷한 색인 서로 다른 부위까지 합쳐버린 것. 다른 3개 보스도 확인해서 인어(청록 머리)/마법사(회색 머리)는 문제없고 히어로도 같은 금발머리 문제가 있는 걸 확인 → 두 보스에만 `colorOverrideRects`(좌표 범위 지정 시 강제 색)를 새로 추가해 머리 영역만 갈색(`#7F5539`)으로 고정, 전체 클러스터링 로직은 그대로 유지(다른 98개 도안 영향 없음). 이후 "옆머리(볼 옆 두 가닥)는 안 바뀜" 재제보로 좌우 영역 좌표도 추가해서 완전히 해결
- 서비스워커 캐시 버전(`CACHE_NAME`)을 매 배포 때마다 올리지 않아서 "폰에 업데이트가 반영 안 된다"는 문제 발생 → v85부터 매 커밋마다 버전을 올리는 걸로 습관화(현재 v97). 새로 추가한 `i18n.js`도 `CRITICAL_SHELL`(필수 캐싱 목록)에 등록

### 맵/표지 화면 3D 입체감 전면 적용
- 레벨박스 미리보기 배지·갤러리 카드 아이콘: 볼록한 유리구슬 스타일(그라디언트 + 안쪽 하이라이트 + 바깥 그림자)
- 모드 4개 버튼·랭킹/모드초기화 버튼·홈/지도 아이콘 버튼: 키캡 스타일(그라디언트+그림자 턱+눌림 애니메이션), 랭킹-모드초기화 간격도 재조정(너무 붙어 보이던 문제)
- 보스 카드 4개(왕관/자물쇠 아이콘 포함), 표지 화면 보스 원형 이미지, 레벨박스/갤러리카드/보스카드 테두리까지 전부 같은 키캡 톤으로 통일
- 표지 보스 좌우 왕복 애니메이션은 시도했다가("서로 간섭 없게, 왕복 30초") 사용자가 "이전이 낫다"고 해서 원래 정적 배치로 되돌림

### 색칠화면 goal/캔버스 경계선 — 여러 번 반복 수정
- 처음엔 점선 → "움직이게" 요청으로 marching-ants 점선 → "점선 말고 방해 안 되는 걸로" 요청에 그라디언트 흐름(보라색) → "그라디언트 움직임이 안 보인다" → 최종적으로 **선은 갈색 울퉁불퉁한 비포장길 느낌(고정)** + 그 위를 **작은 갈색 애벌레가 기어감**
- 애벌레 위치는 장식용 왕복이 아니라 **실제 레벨/보스 타이머 진행률**과 연동(`app.js`의 `setWormProgress()`가 매초 `--worm-progress` CSS 변수 갱신) — 보기만 해도 남은 시간을 느낌으로 알 수 있음. 타이머 없으면 왼쪽 끝에 정지
- 걸음걸이는 매끈한 이동이 아니라 자벌레(inchworm)처럼 늘었다 줄었다 하는 루프(2s 주기)로 별도 애니메이션, 길이도 22→36px로 확대
- goal 확대(🔍) 배지는 도안에 따라 하단이 그림과 겹칠 수 있어 상단 우측으로 이동

### 테스트 유저 피드백 대응
- "좁은 곳이 여전히 잘 색칠이 안 된다" → 탭 인식 스냅 반경(`TAP_SNAP_RADIUS`) 16→26px로 확대
- "10단계까지 지겹다" → 레벨/보상 페이싱 재설계가 필요한 큰 주제라 이번 세션에서는 보류, 다음에 방향 논의 필요

> ⚠️ **기록 공백**: 2026-08-11 세션3 이후 2026-08-21까지(v193~v217 사이) 작업이 이 문서에 기록되지
> 못했습니다 — 보상이미지 탈것 장식/애니메이션, 조각맞추기 미니게임을 전체 레벨로 확장, 유아모드
> 구역별 칭찬(GOOD~PERFECT), 난이도 재설계 논의(`coloring-app-difficulty-redesign.md`), 배경분할/
> 카테고리 확장 문서 등 다수의 커밋이 있었으나 세션 중 히스토리 갱신이 누락됨. 필요하면
> `git log --oneline`으로 그 구간 커밋을 따로 정리할 것.

## 2026-08-21 — 레벨 맵 원형 캐러셀 + 갤러리 개편 + 팔레트/타이머 버그 수정
### 레벨 맵 화면: 사각 박스 → 원형 캐러셀로 전면 개편
- 레벨 노드를 사각 박스에서 큰 원(중앙 45%) + 위/아래 미리보기 원(70% 축소)으로 교체, 위/아래
  드래그(정확히 한 칸씩, 레벨10↔1 원형 순환)로 넘기는 캐러셀 방식으로 변경
- **탭해도 자물쇠 없는(잠금 풀린) 원이 안 열리던 실제 버그 발견/수정**: 드래그용 `setPointerCapture`가
  탭 시 발생하는 `click` 이벤트를 원 버튼이 아니라 `mapGrid` 자신으로 리타겟시켜버려서, 클릭
  리스너가 있는 잠금 풀린 원만 실제로 안 열렸음(잠긴 원은 리스너 자체가 없어서 버그가 안 보였을
  뿐). 눌린 원을 직접 기억해뒀다가 탭 판정 시 `.click()`으로 우회하는 방식으로 수정
- 터치 탭 시 브라우저가 지연 합성하는 호환용 click이 화면 전환 후 엉뚱한 항목을 눌러버리는
  2차 버그도 발견 — `touchstart`에 `preventDefault()` 추가로 해결
- PC는 눌림 효과(`:active`)가 뜨는데 폰에서 안 뜨던 버그 — 위 preventDefault가 터치의 `:active`
  활성화까지 막아버린 게 원인, 수동으로 `lv-pressed` 클래스를 붙였다 떼는 방식으로 대체
- 클리어 체크뱃지가 사각 모서리 기준 위치라 원형 클리핑에 잘려 색조각만 남아 보이던 버그 수정
  (원 중심에서 45도 방향, 반지름 안쪽으로 배지 반지름만큼 뺀 자리로 스냅하는 계산식 적용 —
  갤러리 원형 아이콘의 완료 체크뱃지에도 동일 스타일 재사용)
- 원에 볼록거울 느낌 하이라이트/그림자 오버레이 추가, 진행도(12/12)·타이머를 원 안쪽 위/아래에
  알약칩으로 재배치(캐릭터 그림과 안 겹치게 가장자리에 붙임), 카테고리N 곡선 글자는 임시로 숨김

### 레벨 갤러리(그림 목록) 화면 개편
- 헤더: 뒤로가기 아이콘을 맵 화면과 같은 ⬅️로 통일, 우측에 이전/다음 레벨 아이콘(◀/▶, 마지막
  레벨은 지도 이모지 대신 통일된 ▶) 신설
- "🎉 클리어! 다음▶" 배너를 통째로 삭제해 goal(보상 퍼즐) 이미지 공간 확보, 경과시간 표시는
  제목 밑으로 이동
- **"대기열 2개씩" 카드 시스템을 완전히 폐지** — 레벨 진입 즉시(0% 진행이어도) 실제 그림 +
  빈 자리표시자로 16개(4x4) 채운 원형 아이콘 그리드가 바로 보이고, 그림을 완료해도 그 자리에
  그대로 남는다(대기열처럼 사라지거나 밀리지 않음). 완료 시 보상 이미지로 복제본이 날아가는
  연출은 유지. goal 이미지(퍼즐칸)도 최소 16칸으로 격자를 키움 — 실제 그림이 16개보다 적은
  레벨은 남는 칸이 콘텐츠 16개 채우기 전까진 계속 안 열린 채로 남음(의도된 임시 모습)
- 완료된 원형 아이콘에 지도 화면과 같은 초록 체크(✓) 뱃지 표시

### 팔레트 색 매칭 버그 수정
- 이모지 원본의 실제 색이 팔레트(10색) 밖이면 그 영역을 영원히 못 맞추던 실제 버그 발견 —
  `nearestPaletteColor()`로 실제 화면에 보이는 스와치(`SWATCH_BASE_PALETTE`) 안에서 가장
  가까운 색으로 스냅하도록 수정. 전체 108개 도안(레벨 104 + 보스 4) 전수검사로 팔레트 밖
  색 0개 확인, `validate-all.js`/`verify-full-clear.js` 재검증 통과

### 배터리 절약: 백그라운드 중 타이머 정지
- "게임 중 대기하면 시간이 계속 흘러 배터리를 먹는다" 피드백 — 탭/앱이 백그라운드로 가면
  1초마다 돌던 타이머 갱신 `setInterval`을 멈추고, 다시 보일 때만 재개(`visibilitychange`).
  경과시간 자체는 `Date.now()` 기준 재계산이라 멈췄다 재개해도 정확함

### 다음 단계(사용자 예정)
- 각 카테고리(레벨)를 실제 그림 16개로 채우기 위해 **Twemoji 소스를 먼저 확보한 뒤** 그림을
  하나씩 추가하는 작업 예정 — 오늘 세션은 여기서 마무리

## 2026-08-23 — 챌린지 모드 난이도 선택을 좌우 캐러셀로 교체

### 난이도(쉬움/보통/어려움) 선택 UI 개편
- 기존 EASY/NORMAL/HARD 알약 버튼 3개(Phase 1 임시 스타일)를 지도 화면과 똑같은 원형+홀드링
  캐러셀로 교체 — 좌/중앙/우 3자리, 좌우로 드래그·휠하면 후보가 바뀌고 중앙 원을 700ms 꾹
  눌러야 실제로 선택이 확정된다(지도 화면의 레벨 선택과 동일한 손맛).
- `.lv-circle`/`.lv-circle-stage`/`.lv-peek`/`.lv-hold-ring`류/`.lv-clear-badge`/`.lv-progress`
  등 지도 화면 캐러셀 CSS·마크업을 그대로 재사용하고, 이 캐러셀 전용으로 가로 배치·크기·색만
  덮어씀 — 새 CSS/JS를 거의 새로 안 만들고 기존 걸 재사용한 셈이라 유지보수 부담이 적음.
  난이도별 색은 신호등 관례(쉬움=초록, 보통=주황, 어려움=빨강).
- 레벨 1~10 숫자 그리드는 그대로 유지(이번 범위 밖).

### 버그 2개 — 둘 다 눈에 안 보이게 조용히 깨지는 유형이라 기록해둠
- **CSS 주석 안의 우연한 `*/`**: 새 주석에 `.lv-hold-ring*/.lv-clear-badge`라고 적었는데
  `*` 바로 뒤에 다음 클래스명의 `/`가 붙어 우연히 `*/`(주석 닫기)가 만들어짐 — 주석이 의도보다
  일찍 끝나면서 바로 뒤의 진짜 CSS 규칙(`.diff-node-wrap { display:flex; ... }`)이 깨진 문법으로
  취급돼 브라우저가 그 규칙 전체를 조용히 버림(에러 콘솔에도 안 뜸). **교훈: CSS 주석 안에
  클래스명을 나열할 때 `*`나 `/`가 인접하지 않게 쓸 것**(`.lv-hold-ring 계열`처럼 풀어쓰기).
  `document.styleSheets`의 `cssRules`를 직접 덤프해서 규칙이 통째로 사라진 걸 확인하는 방식으로
  진단함.
- **`max-width`만 있고 `width`가 없는 flex 컨테이너**: `.diff-node-wrap`에 `max-width:420px`만
  주고 `width:100%`를 빠뜨려서, 자식들의 `%` 너비(38%/26%)가 기준 삼을 실제 폭이 없어 전부
  거의 0으로 찌그러짐(순환 참조) — 지도 화면의 `.map-grid`가 이미 `width:100%`를 같이 쓰고
  있던 걸 그대로 안 베껴서 생긴 실수. `width:100%` 추가로 해결.
- 검증: puppeteer로 실제 흐름(챌린지 진입→캐러셀 좌드래그→중앙 원 700ms 홀드→확정 뱃지 확인→
  레벨 그리드 클릭 가능 확인) 전체 재현 + 스크린샷으로 지도 화면(컬러링 모드)도 회귀 없는지 확인.
- `sw.js` `v260`.

### (같은 날 이어서) 실제 스펙 확인 후 구조를 다시 짬 — "난이도 먼저 선택" → "3구역 동시 표시"
- 위 버전을 커밋·푸시한 직후, 사용자가 원한 실제 구조가 달랐음이 확인됨: 난이도를 하나 골라야
  레벨이 보이는 게 아니라, **화면 위/중간/아래에 쉬움/보통/어려움 3구역이 항상 동시에 보이고,
  구역마다 따로 좌우로 스와이프해서 그 난이도의 스테이지 1~10을 넘기는** 구조. 그래서 방금 만든
  "난이도 하나 고르는 좌우 캐러셀 + 그 아래 레벨 1~10 숫자 그리드"를 걷어내고 다시 만듦.
- 구현: 화면을 `.challenge-tiers`(세로 flex) 안에 `.challenge-tier-zone` 3개(flex:1 1 0)로 3등분,
  구역마다 독립된 좌/중앙(큰 원)/우 캐러셀(스테이지 1~10, 원형으로 순환) 배치. 3벌을 복붙하지
  않도록 `createHorizontalHoldCarousel(container, n, renderSlots)` 공용 헬퍼로 뽑아 구역마다 인스턴스
  하나씩 생성 — 드래그/휠/700ms 홀드 로직은 한 군데뿐. 중앙 원을 홀드하면 바로 그 (난이도, 레벨)로
  진입(`state.difficulty` 세팅 후 `startLevel(level)`) — "고르고 나서 또 레벨을 고르는" 2단계가
  아니라 원 하나 홀드로 바로 시작.
- `.screen`의 `min-height:100dvh`(바닥값만 있고 실제 높이는 없음)로는 3구역이 정확히 3등분 안 되고
  페이지가 그냥 늘어나길래, `#challenge-select-screen`에만 `height:100dvh`를 줘서 flex:1 구역들이
  실제 예산을 갖게 함(예전 `#coloring-screen` 정사각형 캔버스 때와 같은 원인). 원 크기는 `container-
  type:size` + `min(Xcqw, Ycqh)`로 구역의 폭/높이 중 더 빡빡한 쪽 기준으로 잡음(폭 기준 %만 쓰면
  세로로 3등분된 좁은 구역에서 원이 위아래로 넘칠 수 있어서).
- 색/원 재사용 방식은 그대로: `data-diff` 값으로 `.lv-circle`의 `--lv-bg`/`--lv-bezel`을 구역 색으로
  바꿔치기(신호등 관례). 이번엔 원 안에 스테이지 숫자를 큼직하게 표시(이모지 미리보기·확정 체크
  뱃지는 없음 — 홀드=바로 시작이라 "확정 상태 표시"가 필요 없어짐).
- 안 쓰게 된 `.challenge-level-grid`/`.challenge-level-btn*` CSS, `IMPLEMENTED_LEVELS` 상수 삭제.
- 검증: puppeteer로 (1) 세 구역이 각각 독립적으로 움직이는지(한 구역만 드래그해도 나머지 안 움직임),
  (2) 중앙 원 700ms 홀드 시 정확한 (난이도, 레벨)로 진입하는지 확인, 스크린샷으로 레이아웃 확인.
- `sw.js` `v261`.

> ⚠️ **기록 공백**: 2026-08-23(세션 위) 이후 2026-08-23 늦은 세션(v262~v304 사이)까지 표지 칠판
> 컨셉 전환, 하단 탭바 5개 신설, 표지·탭바·맵·갤러리·챌린지선택·색칠화면 전체를 어두운 우주
> 테마(성운+별+별똥별)로 전환하는 대규모 작업이 있었으나 세션 중 히스토리 갱신이 누락됨. 필요하면
> `git log --oneline`으로 그 구간 커밋을 따로 정리할 것.

## 2026-08-23~24 — 우주 테마 마무리(성운/별똥별 누락 보완), 네비게이션 버그 3건 수정, 챌린지 리그 테마화

### 우주 테마 마무리 점검
- 챌린지 선택 화면과 색칠 화면에 어두운 배경색(`--bg`)만 깔려 있고, 성운(`::after`)·별똥별
  (JS 주입) 레이어는 지도/갤러리 화면에만 적용돼 있던 걸 발견 — 두 화면도 셀렉터에 추가해서
  전체 6개 화면(표지+탭바5, 지도, 갤러리, 챌린지선택, 색칠)이 다 동일하게 애니메이션 배경을 씀.
- 색칠 화면의 goal 박스(`.goal-canvas-wrap`)/캔버스 박스(`.canvas-square`)는 여러 차례 요청을
  거쳐 최종적으로: **이미지 박스 자체는 고정 흰색(#FFF8ED), 그 바깥 여백(.goal-panel/.canvas-wrap)은
  완전 투명**으로 정리 — 투명해야 뒤 성운/별이 비치고, 흰색 박스여야 그림이 잘 보임(둘 다 동일한
  구조로 통일).
- 하단 탭바(스코어/일일/홈/상점/더보기) 아이콘에 배지 없이 drop-shadow(이미지)/text-shadow
  (더보기 ⋯ 텍스트) 2겹으로 입체감만 추가, 일일 탭 달력 카드는 그림자만 키캡 톤으로 교체.

### 네비게이션 버그 3건
1. **되돌리기(undo) 버튼이 실은 뒤로가기였음**: 색칠 화면 링의 `#btn-back`(⬅️)이 `goHome()`에
   연결돼 있어서 실제로는 화면을 나가는 버튼이었는데, 자리·의도상 되돌리기여야 했다. 탭 히스토리
   스택 기반 `undoLastFill()`을 새로 구현(마지막 채우기 한 칸을 이전 색/미채색으로 되돌림)하고
   여기 연결. 헤더의 `#btn-home`은 진짜 뒤로가기(`goHome()`, 한 단계만)로, 안 쓰게 된
   `goHomeToMap()`은 삭제. 챌린지 모드 중 이 두 버튼을 가로채던 `challenge.js` 로직도 되돌리기는
   더 이상 가로채지 않게 수정(안 그러면 되돌리기가 챌린지 종료로 오작동).
2. **갤러리 헤더 왼쪽 뒤로가기가 무조건 맵으로 나감**: 스테이지2에서 눌러도 스테이지1이 아니라
   스테이지 선택(맵) 화면이 나오던 버그. `openLevel(currentLevel-1)`로 교체(스테이지1에서만
   맵으로 폴백), 기능이 겹치던 오른쪽 ◀ 버튼(`#btn-level-back`)은 삭제, ▶(다음) 버튼 크기를
   왼쪽과 통일(36px→56px).
3. **레벨1 변신영상 도중 뒤로가기 시 검정화면이 다음 레벨까지 남음**: `#level-reward-art`(모자이크
   그림, 레벨마다 새로 안 만들고 공유 재사용)가 변신 영상 재생 시작 시 `hidden` 처리되는데, 영상이
   끝까지 재생돼 `onended`가 불려야만 그 `hidden`이 풀렸다. 영상 도중 나가면 `onended`가 영영 안
   불려서 `hidden`이 계속 남고, 이후 어느 레벨에 들어가도(같은 엘리먼트 재사용) 그림 없이 빈 검정
   박스만 보임 — `openLevel()`의 기존 리셋 블록에 `levelRewardArt.removeAttribute('hidden')`
   한 줄 추가로 해결. Puppeteer로 "16/16 완료→재진입(영상 자동재생)→영상 도중 뒤로가기→레벨2
   진입" 시나리오를 재현해 확인.

### 아이콘/버튼 정리
- 뒤로가기(⬅️)/다음(▶)/되돌리기(↩️)/홈(🏠) 버튼을 시스템 이모지 글자 대신 Twemoji SVG로 통일
  (`leftarrow.svg`/`playbutton.svg`/`rightarrowcurvingleft.svg` 신규 다운로드, `house.svg`는
  기존 걸 재사용) — 기기별 렌더링 차이 제거.
- 컬러링 모드(맵 화면)와 챌린지 모드(챌린지선택 화면)의 홈버튼 크기를 통일(36px→56px), 맵 화면
  리셋 버튼도 같은 크기로.
- 안 쓰게 된 i18n 키 정리(`challenge.selectTitle` 삭제, `mapScreen.backToCover`→`mapScreen.home`).

### 챌린지 모드 난이도 리그 테마화
- 쉬움/보통/어려움 → 마이너리그/메이저리그/챔피언스리그로 변경(영/한/일/중/스 5개 언어 전부
  번역), 라벨 글자 크기 3배(0.8rem→2.4rem), 글꼴은 다른 곳(레벨 원 숫자 등)과 같은 Fredoka+한글
  대체 스택으로 통일.
- 챌린지 선택 화면 3개 리그 구역마다 우측상단에 리셋 버튼 신규 추가 — `challengeBestScore`에서
  그 난이도 칸만 지움(다른 난이도 기록엔 영향 없음, 테스트로 확인).

### 다음 단계
- 위 "기록 공백" 구간(v262~v304) 커밋 로그 정리는 아직 안 함.
- 리셋 버튼을 라벨과 같은 줄(flex)로 묶어서 높이(수직 중앙)를 정확히 맞춤(처음엔 구역 전체
  기준 절대좌표라 라벨보다 위에 떠 보였음).
- `sw.js` `v304` → `v310`.
